import SwiftUI

extension Color {

    // MARK: - Primary palette

    /// Warm paper background — #f4efe6
    static let warmPaper = Color(red: 0xF4 / 255, green: 0xEF / 255, blue: 0xE6 / 255)

    /// Deep ink for primary text — #1a1713
    static let deepInk = Color(red: 0x1A / 255, green: 0x17 / 255, blue: 0x13 / 255)

    /// Terracotta/ochre accent — #a85a2a
    static let terracotta = Color(red: 0xA8 / 255, green: 0x5A / 255, blue: 0x2A / 255)

    // MARK: - Secondary tones

    /// Parchment — #ebe4d6
    static let parchment = Color(red: 0xEB / 255, green: 0xE4 / 255, blue: 0xD6 / 255)

    /// Aged linen — #e0d7c3
    static let agedLinen = Color(red: 0xE0 / 255, green: 0xD7 / 255, blue: 0xC3 / 255)

    /// Charcoal — #3a342b
    static let charcoal = Color(red: 0x3A / 255, green: 0x34 / 255, blue: 0x2B / 255)

    /// Stone — #6b6355
    static let stone = Color(red: 0x6B / 255, green: 0x63 / 255, blue: 0x55 / 255)

    /// Sandstone — #d6cdb9
    static let sandstone = Color(red: 0xD6 / 255, green: 0xCD / 255, blue: 0xB9 / 255)

    /// Burnt sienna — #7a3f1c
    static let burntSienna = Color(red: 0x7A / 255, green: 0x3F / 255, blue: 0x1C / 255)
}
